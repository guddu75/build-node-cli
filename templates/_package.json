{
    "name": "{{name}}",
    "description": "{{description}}",
    "version": "{{version}}",
    "bin": {
        "{{name}}" : "index.js",
        "{{command}}" : "index.js"
    },
    "author": {
        "name": "{{authorName}}",
        "email": "{{authorEmail}}"
    },
    "license": "{{license}}"
}